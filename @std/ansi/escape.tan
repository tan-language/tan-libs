; #todo find a proper path:
; - /ansi/escape
; - /codec/ansi/escape

; #todo convert from html to ansi-escaped-text

; automatically, (and lazily?) generate the color functions!

; #todo problem this is not composeable! how to have red+bold? -> Generate a ANSI-String-Expr thats rendered at the end, similar to HTML-Expr

; example: (writeln "hello " (a/red "George") ", how do you feel?")
(let red (Func [s]
    "\x1b[31m${s}\x1b[0m"
))
